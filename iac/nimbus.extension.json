{
  "id": "com.nimbus.iac",
  "displayName": "Infrastructure-as-Code",
  "version": "0.1.0",
  "description": "Terraform / Pulumi / OpenTofu CLI helpers. The connector itself opens no sockets; CLI subprocesses' network access is sandbox-bounded. Workspace paths are tool-call scoped (workingDirectory argument), not baked into the manifest. Mutations require Gateway HITL.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": []
  },
  "hitlRequired": ["write", "delete"],
  "minNimbusVersion": "0.2.0"
}
