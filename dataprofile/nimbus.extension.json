{
  "id": "com.nimbus.dataprofile",
  "displayName": "Local Data Profiling",
  "version": "0.1.0",
  "description": "Local data profiling connector (Tier-5 local, no-row-data class). Profiles local data files — `.parquet`, `.csv`, `.jsonl`/`.ndjson`, `.json` — under a configured directory into `dataprofile:data_model` items carrying the SCHEMA only: column names, column types, column count, a row-count ESTIMATE, and file size. It NEVER indexes cell values, row samples, first-N-row previews, or header-row data values. Parquet schema + row count come from the file FOOTER metadata (read via hyparquet — no row data crosses the wire); CSV column names come from the header line; JSONL/JSON field names + JS types come from the top-level structure (keys/types only). It is a pure, bounded, path-traversal-guarded filesystem read: NO database, NO network, NO row-fetch or row-sample tool (a contract test asserts the surface has none). The user points the connector at the directory holding their data files via the non-secret path `dataprofile.dir`; read tools are `dataprofile_list`, `dataprofile_get`, `dataprofile_search`. The configured directory arrives as the scoped environment variable DATAPROFILE_DIR and is added to the sandbox filesystem.read allow-list at spawn time. No network access. (ORC is deferred — no maintained pure-JS ORC schema reader exists.)",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": [],
    "filesystem": { "read": [], "write": [] }
  },
  "hitlRequired": [],
  "syncInterval": 600,
  "minNimbusVersion": "0.2.0"
}
