{
  "id": "com.nimbus.kubernetes",
  "displayName": "Kubernetes",
  "version": "0.1.0",
  "description": "kubectl CLI connector (pods, deployments, services). API server hostname lives inside kubeconfig YAML; parsing it at spawn time to extend permissions.network is a known Task 14 follow-up. Mutations require Gateway HITL.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": []
  },
  "hitlRequired": ["write", "delete"],
  "syncInterval": 120,
  "minNimbusVersion": "0.2.0"
}
