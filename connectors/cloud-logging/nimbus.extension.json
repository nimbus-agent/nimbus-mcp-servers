{
  "id": "com.nimbus.cloud-logging",
  "displayName": "GCP Cloud Logging",
  "version": "0.1.0",
  "description": "GCP Cloud Logging metadata connector (Tier-3, no-row-data). Indexes log routing SINK configuration metadata only — sink name, destination, filter expression, description, disabled flag, and create/update timestamps — via the gcloud CLI's `gcloud logging sinks list/describe` commands. Never reads log ENTRIES / row data: there is no `gcloud logging read` and no `gcloud logging entries list`. Reuses the existing GCP service-account credentials via the gcloud CLI. Read-only; no write tools.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": ["logging.googleapis.com"]
  },
  "hitlRequired": [],
  "syncInterval": 600,
  "minNimbusVersion": "0.2.0"
}
