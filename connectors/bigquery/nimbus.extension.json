{
  "id": "com.nimbus.bigquery",
  "displayName": "BigQuery",
  "version": "0.1.0",
  "description": "Google BigQuery metadata connector (Tier-3, no-row-data). Indexes datasets, tables, and schema field names/types via the BigQuery REST metadata API — never row, cell, or query-result data. Reuses the existing GCP service-account credentials via the gcloud CLI. Read-only; no write tools.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": ["bigquery.googleapis.com", "oauth2.googleapis.com", "www.googleapis.com"]
  },
  "hitlRequired": [],
  "syncInterval": 600,
  "minNimbusVersion": "0.2.0"
}
