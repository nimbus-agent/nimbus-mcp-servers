{
  "id": "com.nimbus.gitlab",
  "displayName": "GitLab",
  "version": "0.1.0",
  "description": "GitLab REST connector (projects, merge requests, pipelines). Self-hosted hosts come from gitlab.api_base. Mutations require Gateway HITL.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": ["gitlab.com"]
  },
  "hitlRequired": ["write", "delete"],
  "syncInterval": 300,
  "minNimbusVersion": "0.2.0"
}
