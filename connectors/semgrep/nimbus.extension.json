{
  "id": "com.nimbus.semgrep",
  "displayName": "Semgrep",
  "version": "0.1.0",
  "description": "Semgrep AppSec Platform connector (SAST findings, rule matches, triage state). Read-only; surfaces severity, rule_id, file/line, repository, triage_state into the local index. Self-hosted Semgrep Enterprise instances are a known Task 14 runtime-merge follow-up — the SaaS host (semgrep.dev) is the only one in the static sandbox manifest today.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": ["semgrep.dev"]
  },
  "hitlRequired": [],
  "syncInterval": 600,
  "minNimbusVersion": "0.2.0"
}
