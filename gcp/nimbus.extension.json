{
  "id": "com.nimbus.gcp",
  "displayName": "Google Cloud",
  "version": "0.1.0",
  "description": "GCP CLI connector (IAM control plane + OAuth). Service-specific googleapis.com hosts (storage, bigquery, etc.) are a known Task 14 follow-up. Mutations require Gateway HITL.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": ["cloudresourcemanager.googleapis.com", "oauth2.googleapis.com"]
  },
  "hitlRequired": ["write", "delete"],
  "syncInterval": 600,
  "minNimbusVersion": "0.2.0"
}
