{
  "name": "nimbus-mcp-zoom",
  "version": "0.1.0",
  "private": true,
  "description": "Zoom REST API connector — read-only access to scheduled meetings",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@nimbus-dev/sdk": "^1.19.0",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
