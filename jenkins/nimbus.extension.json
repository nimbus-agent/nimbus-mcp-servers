{
  "id": "com.nimbus.jenkins",
  "displayName": "Jenkins",
  "version": "0.1.0",
  "description": "Jenkins CI connector (jobs, builds, queue). Always self-hosted — the target host is read from jenkins.base_url at spawn time and merged into permissions.network by the Gateway. Mutations require Gateway HITL.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": []
  },
  "hitlRequired": ["write", "delete"],
  "syncInterval": 120,
  "minNimbusVersion": "0.2.0"
}
