{
  "id": "com.nimbus.sonarqube",
  "displayName": "SonarQube / SonarCloud",
  "version": "0.1.0",
  "description": "SonarQube + SonarCloud REST connector (issues, projects). Read-only; surfaces severity, type (BUG/VULNERABILITY/CODE_SMELL), rule, file/line into the local index. Self-hosted SonarQube via `sonarqube.url` is a known Task 14 runtime-merge follow-up — the SaaS host (sonarcloud.io) is the only one in the static sandbox manifest today.",
  "author": "Nimbus",
  "entrypoint": "dist/server.js",
  "runtime": "bun",
  "permissions": {
    "network": ["sonarcloud.io"]
  },
  "hitlRequired": [],
  "syncInterval": 600,
  "minNimbusVersion": "0.2.0"
}
